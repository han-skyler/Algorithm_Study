#include <iostream>
using namespace std;

int main() {
    int buta_year;
    cin >> buta_year;
    if (1000 <= buta_year && buta_year <= 3000) {
        cout << buta_year - 543 << endl;
    }
    return 0;
}
